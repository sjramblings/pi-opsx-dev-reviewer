# Documentation delta — {{change}}

Written by the `tech-writer` subagent. Covers every user-visible change in this change
folder. Structured by Diátaxis (one mode per page), styled to pass `just docs-lint`,
verified against the shipped code.

## What changed for the reader

<!-- The user-facing delta: new/changed commands, flags, config, endpoints, behaviour. -->

## Diátaxis map

<!-- Which docs page(s) this touches and which quadrant each is:
     tutorial | how-to | reference | explanation. One mode per page. -->

## Verification

<!-- `just docs-lint` output (clean), plus the code facts checked to prevent drift. -->
