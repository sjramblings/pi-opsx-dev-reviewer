# Tasks—add-rukas-grafts

> Five independent gates ported from pi-rukas. Groups 1 to 5 touch disjoint code files but
> share `justfile.opsx` and `README.md`, so run them sequentially.

## 1. Fix-loop cap (spec: fix-loop-cap)

- [ ] 1.1 Count BLOCK rounds per task in `record-verdict` and append a `PARKED:` entry with
      exit 3 at the cap (`OPSX_MAX_BLOCK_ROUNDS`, default 3, invalid fails closed).
      files: `tools/record-verdict.ts`, `tools/record-verdict.test.ts`
      probe: `bun test tools/record-verdict.test.ts` passes, including a third-BLOCK park test, an under-cap continue test, and an invalid-cap test.
      out-of-scope: the `record-verdict` recipe line (recipe thinness is asserted).
      spec: `fix-loop-cap`
      parallel: a

- [ ] 1.2 Make `/opsx-loop` stop on a `parked` result instead of re-dispatching.
      files: `prompts/opsx-loop.md`, `openspec/schemas/dev-reviewer/schema.yaml`, `openspec/apply-policy.config.yaml`
      probe: `git grep -n "parked" -- prompts/opsx-loop.md openspec/schemas/dev-reviewer/schema.yaml openspec/apply-policy.config.yaml` shows the stop instruction in all three BLOCK surfaces.
      out-of-scope: generated `.pi/` and `.cursor/` apply skills (regenerated by openspec).
      spec: `fix-loop-cap`
      parallel: none (after 1.1)

## 2. Diff gate (spec: diff-gate)

- [ ] 2.1 Add `tools/diff-gate.ts` with a net skip-marker check and a falsely-green check, and
      a `just diff-gate` recipe.
      files: `tools/diff-gate.ts`, `tools/diff-gate.test.ts`, `justfile.opsx`, `install.sh`
      probe: `bun test tools/diff-gate.test.ts` passes, covering added skip, moved skip, string-literal marker, falsely-green, and docs-only exemption.
      out-of-scope: wiring diff-gate into verify-gate.
      spec: `diff-gate`
      parallel: b

## 3. Repeat-call detector (spec: repeat-call-detector)

- [ ] 3.1 Add the `repeat-call-detector` extension with its tests.
      files: `extensions/repeat-call-detector/index.ts`, `extensions/repeat-call-detector/index.test.ts`
      probe: `bun test extensions/repeat-call-detector` passes (eighth call blocked, reset on distinct call, key order) and `just check-extensions` reports clean.
      out-of-scope: path redaction.
      spec: `repeat-call-detector`
      parallel: c

## 4. Pi compatibility (spec: pi-compatibility)

- [ ] 4.1 Add `docs/pi-compatibility.md`, `tools/pi-compat.ts` with its drift test, and a
      `just pi-compat` recipe.
      files: `docs/pi-compatibility.md`, `tools/pi-compat.ts`, `tools/pi-compat.test.ts`, `justfile`
      probe: `bun test tools/pi-compat.test.ts` passes, including a real-repo drift check returning no findings and a disagreeing-claim fixture that is reported; `just pi-compat` exits 0.
      out-of-scope: a live Pi event-shape test.
      spec: `pi-compatibility`
      parallel: d

## 5. Claim scan and agent wiring (spec: docs-claim-scan)

- [ ] 5.1 Add `tools/claim-scan.ts` with tests and a `just claim-scan` recipe.
      files: `tools/claim-scan.ts`, `tools/claim-scan.test.ts`, `justfile.opsx`, `install.sh`
      probe: `bun test tools/claim-scan.test.ts` passes for invented size, backed version, and missing path.
      out-of-scope: judging whether a backed claim is true.
      spec: `docs-claim-scan`
      parallel: e

- [ ] 5.2 Admit `just diff-gate` and `just claim-scan` in the reviewer's gated bash, and tell
      the reviewer and tech-writer to run them.
      files: `extensions/architect-scope/index.ts`, `extensions/architect-scope/index.test.ts`, `agents/reviewer.md`, `agents/tech-writer.md`
      probe: `bun test extensions/architect-scope` passes with new cases allowing both recipes for the reviewer and still blocking a chained write.
      out-of-scope: the spec-reviewer allowlist.
      spec: `docs-claim-scan`
      parallel: none (after 2.1, 5.1)

## 6. Documentation

- [ ] 6.1 Document the five gates in `README.md` and `index.html`, and record the decision as
      an ADR.
      files: `README.md`, `index.html`, `docs/decisions/0006-port-pi-rukas-gates.md`
      probe: `bun test tools/docs-contracts.test.ts` passes and `bunx --bun markdownlint-cli2@0.23.1 "**/*.md"` reports 0 issues.
      out-of-scope: `docs/architecture/**` (owned by architecture-writer).
      spec: `docs-claim-scan`
      parallel: none (after 1 to 5)
